# =============================================================================
# requirements-dev.txt — DEPENDÊNCIAS PARA DESENVOLVIMENTO E INGESTÃO LOCAL
# =============================================================================
#
# Instale com: make install
#
# Inclui tudo para:
#   - Escrever e rodar testes
#   - Formatar e checar estilo
#   - Rodar o pipeline de ingestão localmente (make ingest wave=2)
#
# O pipeline de ingestão roda na máquina local com IP residencial brasileiro —
# o cedoc/ bloqueia IPs de datacenter. curl_cffi mimetiza o TLS do Chrome.
# =============================================================================

# --- Ingestão de dados ---
requests>=2.32           # HTTP para Power BI, GitLab, Planalto
curl_cffi>=0.7           # download do cedoc/ — mimetiza TLS do Chrome (Cloudflare Bot Management)
beautifulsoup4>=4.12     # parsing de HTML (leis do planalto.gov.br)
lxml>=5.2                # backend rápido do BeautifulSoup
PyMuPDF>=1.24            # extração de texto de PDFs — estratégia baseline (importado como fitz)
pymupdf4llm>=0.0.17      # extração de PDFs em Markdown — 2ª estratégia do benchmark
python-docx>=1.1         # manuais DOCX
openpyxl>=3.1            # manuais XLSX
tqdm>=4.66               # barras de progresso

# --- Dados e publicação ---
pandas>=2.2
pyarrow>=16.0            # leitura/escrita de Parquet
huggingface_hub>=0.23    # upload para o HF Hub
datasets>=2.20           # leitura do corpus publicado
python-dotenv>=1.0       # carrega variáveis do .env

# --- Ferramentas de desenvolvimento ---
pytest>=8.2
black>=24.0
flake8>=7.0
jupyter>=1.0
rank-bm25>=0.2.2         # necessário para imports nos testes de RAG
