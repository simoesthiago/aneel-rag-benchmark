# =============================================================================
# requirements-ingest.txt — só Camada 1 (GitHub Actions / Colab ingestão)
# =============================================================================
# Evita instalar torch/sentence-transformers (~3 GB) no workflow de ingestão.
# Para dev completo: requirements-dev.txt
# =============================================================================

requests>=2.32
curl_cffi>=0.7
beautifulsoup4>=4.12
lxml>=5.2
PyMuPDF>=1.24
tqdm>=4.66
pandas>=2.2
pyarrow>=16.0
huggingface_hub>=0.23
datasets>=2.20
python-dotenv>=1.0
