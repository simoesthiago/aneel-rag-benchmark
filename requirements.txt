# =============================================================================
# requirements.txt — DEPENDÊNCIAS DO PROJETO
# =============================================================================
#
# Instale com: make install
#
# Organizado por camada. "CAMADA 1" e "DESENVOLVIMENTO" estão confirmados
# e instalados hoje. As demais camadas estão comentadas: deps confirmadas
# no stack são marcadas como [confirmado]; deps ainda sem decisão final
# são marcadas como [a escolher] com os candidatos listados.
# =============================================================================


# =============================================================================
# CAMADA 1 — INGESTÃO (instalado)
# =============================================================================

# --- Scraping e HTTP ---
requests>=2.32           # HTTP para Power BI, GitLab, Planalto
curl_cffi>=0.7           # download do cedoc/ — mimetiza TLS do Chrome (Cloudflare)
beautifulsoup4>=4.12     # parsing de HTML (portal ANEEL + planalto.gov.br)
lxml>=5.2                # backend rápido do BeautifulSoup
tqdm>=4.66               # barras de progresso para scraping longo

# --- Extração de documentos ---
PyMuPDF>=1.24            # PDF → texto (baseline, importado como fitz)
pymupdf4llm>=0.0.17      # PDF → Markdown (segunda estratégia do benchmark)
html2text>=2024.2        # HTML → Markdown; também usado pelo mammoth para DOCX
python-docx>=1.1         # DOCX → texto (manuais DOCX)
mammoth>=1.8             # DOCX → HTML → Markdown via html2text
openpyxl>=3.1            # XLSX → texto (manuais XLSX)
tabulate>=0.9            # backend de DataFrame.to_markdown() — XLSX → Markdown

# --- Dados e publicação ---
pandas>=2.2              # manipulação tabular e XLSX → Markdown
pyarrow>=16.0            # leitura/escrita de Parquet
huggingface_hub>=0.23    # upload do corpus para o HF Hub
datasets>=2.20           # leitura do corpus publicado
python-dotenv>=1.0       # carrega variáveis do .env


# =============================================================================
# DESENVOLVIMENTO (instalado)
# =============================================================================

pytest>=8.2
black>=24.0
flake8>=7.0
jupyter>=1.0             # notebooks exploratórios em notebooks/


# =============================================================================
# CAMADA 2 — PROCESSAMENTO
# =============================================================================
#
# Embedding via API foi definido como caminho principal da Camada 2.2.

openai>=1.35                  # OpenAI embeddings API
tiktoken>=0.7                 # contagem/truncamento seguro de tokens para OpenAI
faiss-cpu>=1.8                # vector store: arquivo .faiss, sem servidor, portável para HF Spaces
numpy>=1.26                   # arrays float32 para FAISS (já dep transitiva, pinned aqui para clareza)


# =============================================================================
# CAMADA 3 — RAG
# =============================================================================
#
# LangChain como orquestrador e OpenAI como LLM estão confirmados no stack
# (ver CLAUDE.md). Versões exatas a fixar quando a camada começar.
#
# langchain>=0.2               # [confirmado] orquestração de RAG
# langchain-community>=0.2     # [confirmado] integrações (FAISS, loaders, etc.)
# openai>=1.35                 # [confirmado] LLM principal

tenacity>=9.0                  # retry+backoff nas chamadas OpenAI (Sprint 6)
cohere>=5.11                   # reranker Cohere Rerank 3 (Sprint 7)


# =============================================================================
# CAMADA 5 — INTERFACE (ainda não instalado)
# =============================================================================
#
# Streamlit está definido como framework do chatbot no HF Spaces (ver CLAUDE.md).
#
# streamlit>=1.36              # [confirmado] chatbot público no HF Spaces
